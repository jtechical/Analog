# Uncomment the next line to define a global platform for your project
 platform :ios, '12.1'

target 'Analog' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Analog
  # My opinion is that pods should not be added to git, but the major version or private repo should be indicated
    pod 'RxSwift', '~> 6.0'
    pod 'RxCocoa', '~> 6.0'
    pod 'SwiftLint', '~> 0.4'
end
